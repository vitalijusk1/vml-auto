# Car Parts Inventory Dashboard

A dashboard application for car parts selling companies to manage inventory, track sales, and analyze business performance.

## How to Run

1. Install dependencies: `npm install`
2. Start the development server: `npm run dev`
3. Open your browser at the URL shown in the terminal (usually `http://localhost:5173`)
